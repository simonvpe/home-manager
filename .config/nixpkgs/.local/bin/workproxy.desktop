#!/usr/bin/env bash


if [[  $1 == start ]]; then
    exec bash $0 none
elif [[ $1 == stop ]]; then
    pkill -f 'sleep infinity'
elif [[ $1 == none ]]; then
    ssh -f workproxy sleep infinity
    ssh -f worksocks sleep infinity
else
    >&2 echo "Usage: $0 start|stop"
fi
